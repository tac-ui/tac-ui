/**
 * Motion constants for React Native components.
 * Re-exports from @tac-ui/tokens to avoid duplication.
 */
export { tacSpring, springConfigs } from '@tac-ui/tokens/native';
import { motionTokens } from '@tac-ui/tokens/native';

/** Duration values in milliseconds — derived from tokens */
export const duration = motionTokens.duration;
